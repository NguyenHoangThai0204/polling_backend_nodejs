Vào thư mục:
    npm init 
    dùng Hugging face để ai tự kiểm duyệt nội
    npm install --save-dev typescript @types/node @types/express @types/body-parser @types/cors @types/morgan
    npm i axios
    npm install mongodb
     npm install aws-sdk
tạo file index.js để chạy

git reset --hard HEAD~2


chế độ tự động xoá
npm install node-cron
